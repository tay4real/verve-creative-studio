<?php
/**
 * Page: Flow A — Service/Photography Type selection
 * Status: BLOCKED / RESERVED — not part of the Wedding Film & Photography sequence (that flow's
 * design shows only 10 steps with no type-selection step, since Wedding is already a fixed
 * service). This step may still be needed for a *generic Photography* booking entry point,
 * which would ask visitors to pick a sub-type (Portrait, Event, Commercial, Real Estate, etc.)
 * — confirm with client whether that flow exists separately before building this.
 */
