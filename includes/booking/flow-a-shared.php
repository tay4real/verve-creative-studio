<?php
/**
 * Step order + labels for the Flow A (Wedding Film & Photography / Photography) wizard.
 * Single source of truth — the progress bar in flow-a-header.php reads this, so adding/removing
 * a step means editing this array once rather than every step file's markup.
 */
$flow_a_steps = ['Coverage', 'Duration', 'Date', 'Time', 'Location', 'Add-ons', 'About Event', 'Upload', 'Review', 'Payment'];
